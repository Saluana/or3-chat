# Tasks (Feb 23) - or3-chat

- [x] **Validate new presign inputs**
  - Location: `server/api/storage/presign-upload.post.ts`, `server/api/storage/presign-download.post.ts`
  - Details: `expires_in_ms` and `disposition` are unbounded free-form values. Validate in core schema before dispatch: integer range for expiry and strict disposition grammar/length.
- [x] **Fix Wizard S3 TTL validation**
  - Location: `shared/cloud/wizard/validation.ts`
  - Details: Wizard only emits warnings for TTL outside practical bounds and never enforces integer/range constraints. Make S3 TTL validation authoritative in wizard (`int`, `1..86400`) and use errors, not warnings, for invalid bounds.
- [x] **Fix Web wizard deploy skipping Convex env bootstrap**
  - Location: `server/wizard/index.ts`, `shared/cloud/wizard/deploy.ts`
  - Details: The web/API deploy path never calls `applyConvexEnv`. Invoke `applyConvexEnv` inside `runWizardDeploy` (and any shared deploy path), gate it on provider selections, respect `dryRun`, and surface failures as hard deploy errors.
- [x] **Fix Secret cache re-injecting deleted credentials**
  - Location: `shared/cloud/wizard/api.ts`
  - Details: `transientSessionSecrets` is merged on write but never pruned. Replace merge-with-old behavior with replace-from-current; delete missing keys and delete the map entry when no secrets remain.
- [x] **Fix Wizard auth dying after cookie TTL expires**
  - Location: `app/wizard/composables/useWizardSession.ts`, `server/middleware/wizard-token-auth.ts`
  - Details: The client is wired to send `x-wizard-token` from `sessionStorage`, but never stores that token. Persist the token on first load (query/cookie bootstrap) and always include `x-wizard-token`; alternatively refresh grant-cookie TTL on wizard API traffic.
- [x] **Fix Theme generator writing invalid TypeScript for normal input**
  - Location: `scripts/cli/create-theme.ts`
  - Details: User-provided strings are interpolated into quoted template output without escaping. Use safe serialization (`JSON.stringify`) for string interpolation in generated TS and docs.
- [x] **Fix Failed connection test discarding user-entered step data**
  - Location: `scripts/cli/or3-cloud.ts`
  - Details: On connection-test failure with "do not bypass", control jumps before `submitAnswers` executes. Keep the user patch persisted before retrying the test, or loop only inside the connection-test block so the step state is retained.
- [x] **Fix Sandbox script coupling to a non-portable folder layout**
  - Location: `scripts/cli/create-temp-sandbox.ts`
  - Details: The default source path hardcodes a sibling `or3-sandbox/sandbox3` directory and fails hard if absent. Treat missing default source as non-fatal; require explicit `--source` or vendor a template inside the repo.
- [x] **Connect Wizard form labels to inputs**
  - Location: `app/wizard/components/WizardFieldRenderer.vue`
  - Details: `<label>` elements are visually present, but controls do not receive linked `id`/`for` relationships. Generate stable per-field IDs; bind label `for`, input `id`, and `aria-describedby` for helper text.
- [x] **Add real CLI behavior validation to “CLI commands” tests**
  - Location: `scripts/__tests__/cli-commands.test.ts`
  - Details: Tests mostly exercise helper/compiler behavior and never execute the real wizard entrypoint flows. Add process-level tests that run `scripts/cli/or3-cloud.ts` and sibling script commands in temp dirs with assertions on exit status, output, and generated artifacts.
- [x] **Make Theme preview modal a real dialog**
  - Location: `app/wizard/components/WizardStepThemes.vue`
  - Details: Overlay opens visually but lacks dialog semantics/focus trap. Add `role="dialog"` + `aria-modal="true"` and enforce focus trapping while open.
