# Background Tool Execution - Visual Architecture

## System Overview Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         OR3 Chat Architecture                            │
│                    Background Tool Execution Flow                        │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Client     │
│  (Browser)   │
└──────┬───────┘
       │
       │ 1. POST /api/openrouter/stream
       │    { _background: true, tools: [...] }
       ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         Server Side                                   │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Background Stream Handler                                    │  │
│  │  (server/utils/background-jobs/stream-handler.ts)            │  │
│  │                                                               │  │
│  │  ┌─────────────────────────────────────────────────────┐    │  │
│  │  │  consumeBackgroundStream()                           │    │  │
│  │  │                                                       │    │  │
│  │  │  for await (evt of parseOpenRouterSSE(stream)) {    │    │  │
│  │  │    if (evt.type === 'text') {                        │    │  │
│  │  │      ✅ Update job content                           │    │  │
│  │  │      ✅ Emit SSE delta                               │    │  │
│  │  │    }                                                  │    │  │
│  │  │    else if (evt.type === 'tool_call') {             │    │  │
│  │  │      ⚠️  MISSING: Accumulate tool calls             │    │  │
│  │  │    }                                                  │    │  │
│  │  │  }                                                    │    │  │
│  │  │                                                       │    │  │
│  │  │  ⚠️ MISSING: if (toolCalls.length > 0) {            │    │  │
│  │  │    executeToolLoop(...)                              │    │  │
│  │  │  }                                                    │    │  │
│  │  └─────────────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ⚠️  NEW COMPONENT NEEDED:                                           │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Tool Executor                                                │  │
│  │  (server/utils/background-jobs/tool-executor.ts)            │  │
│  │                                                               │  │
│  │  ┌─────────────────────────────────────────────────────┐    │  │
│  │  │  executeToolLoop(params)                             │    │  │
│  │  │                                                       │    │  │
│  │  │  while (round < MAX_ROUNDS) {                        │    │  │
│  │  │    1. Stream from OpenRouter                         │    │  │
│  │  │    2. If tool_calls:                                 │    │  │
│  │  │       - Execute tools                                │    │  │
│  │  │       - Append results to conversation               │    │  │
│  │  │       - Continue loop                                │    │  │
│  │  │    3. If stop:                                       │    │  │
│  │  │       - Return final content                         │    │  │
│  │  │  }                                                    │    │  │
│  │  └─────────────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                              │                                        │
│                              ▼                                        │
│  ⚠️  NEW COMPONENT NEEDED:                                           │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Server Tool Registry                                         │  │
│  │  (server/utils/tools/tool-registry.ts)                      │  │
│  │                                                               │  │
│  │  Map<string, ServerToolDefinition>                           │  │
│  │  ┌────────────────────────────────────────────────────┐     │  │
│  │  │ 'get_current_time' → handler: () => Date.now()     │     │  │
│  │  │ 'calculate'        → handler: (expr) => eval(expr)  │     │  │
│  │  │ 'web_search'       → handler: (q) => fetch(...)     │     │  │
│  │  └────────────────────────────────────────────────────┘     │  │
│  │                                                               │  │
│  │  executeTool(name, args, context) {                          │  │
│  │    1. Get tool from registry                                 │  │
│  │    2. Validate args                                          │  │
│  │    3. Execute with timeout (10s)                             │  │
│  │    4. Return result or error                                 │  │
│  │  }                                                            │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                              │                                        │
│                              ▼                                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Job Provider (Convex/Memory)                                 │  │
│  │  (server/utils/background-jobs/providers/)                   │  │
│  │                                                               │  │
│  │  ⚠️  NEEDS EXTENSION:                                         │  │
│  │  - activeToolCalls: [{ id, name, status, ... }]             │  │
│  │  - toolRounds: [{ round, toolCalls: [...] }]                │  │
│  │  - workflowState: { nodeStates, executionOrder, ... }       │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                              │                                        │
│                              ▼                                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Database (Convex)                                            │  │
│  │                                                               │  │
│  │  background_jobs {                                            │  │
│  │    id, userId, threadId, messageId,                          │  │
│  │    status, content, chunksReceived,                          │  │
│  │    ⚠️  NEW: activeToolCalls, toolRounds, workflowState       │  │
│  │  }                                                            │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

## Problem Visualization

### Current Flow (Broken)
```
User Message → OpenRouter API → SSE Stream
                                     │
                                     ├─ text events → ✅ Handled
                                     │                   (stored in DB)
                                     │
                                     └─ tool_call events → ❌ IGNORED
                                                          (discarded)
                                                          
Result: Incomplete response (AI asks to use tool, but tool never runs)
```

### Fixed Flow
```
User Message → OpenRouter API → SSE Stream
                                     │
                                     ├─ text events → ✅ Handled
                                     │
                                     └─ tool_call events → ✅ Accumulated
                                                              │
                                                              ▼
                                                    Execute Tool Loop
                                                              │
                                     ┌────────────────────────┴─────────────────────┐
                                     │                                               │
                                     ▼                                               ▼
                           Execute Tools                              Resume Streaming
                           (Server Tool Registry)                     with Tool Results
                                     │                                               │
                                     │                                               │
                                     └──────────────┬────────────────────────────────┘
                                                    │
                                                    ├─ More tool_calls? → Repeat
                                                    │
                                                    └─ finish_reason: 'stop' → Complete
                                                    
Result: Complete response with tool execution results
```

## Memory Leak Visualization

### Before (Leaks)
```
Job 1 Complete → jobStreams.set('job1', state)
                 state.listeners = [] (empty)
                 ❌ Never cleaned up!
                 
Job 2 Complete → jobStreams.set('job2', state)
                 state.listeners = [] (empty)
                 ❌ Never cleaned up!
                 
...after 100 jobs...

jobStreams.size = 100  ❌ Memory leak!
Memory usage: ~100MB
```

### After (Fixed)
```
Job 1 Complete → jobStreams.set('job1', state)
                 state.listeners = [] (empty)
                 ✅ Immediately deleted!
                 
Job 2 Complete → jobStreams.set('job2', state)
                 state.listeners = [] (empty)
                 ✅ Immediately deleted!
                 
...after 100 jobs...

jobStreams.size = 0  ✅ No leak!
Memory usage: Stable
```

## Performance Optimization Visualization

### SSE Broadcast - Before (Slow)
```
OpenRouter → Chunk 1 → SSE Event 1 → Client 1
                    → SSE Event 1 → Client 2
                    → SSE Event 1 → Client 3
                    ...
          → Chunk 2 → SSE Event 2 → Client 1
                    → SSE Event 2 → Client 2
                    → SSE Event 2 → Client 3
                    ...
          (100 chunks = 300 SSE events for 3 clients)
          
CPU: ████████████████ (high)
```

### SSE Broadcast - After (Fast)
```
OpenRouter → Chunks 1-10 → Batched → SSE Event → Client 1
                                                → Client 2
                                                → Client 3
          → Chunks 11-20 → Batched → SSE Event → All clients
          ...
          (100 chunks = 30 SSE events for 3 clients)
          
CPU: ████ (low)
```

## Implementation Priority Map

```
Priority 1 (Core Functionality):
┌─────────────────────────────────────────────────────────────┐
│ Phase 1: Server Tool Registry          [8-12h]              │
│ Phase 2: Database Schema Extensions    [4-6h]               │
│ Phase 3: Tool Execution Engine         [12-16h]             │
│ Phase 4: Stream Handler Integration    [6-8h]               │
└─────────────────────────────────────────────────────────────┘
Total: 30-42 hours (1 week)

Priority 2 (Stability):
┌─────────────────────────────────────────────────────────────┐
│ Phase 5: Memory Leak Fixes             [4-6h]               │
│ Phase 6: Performance Optimizations     [6-8h]               │
│ Phase 9: Testing & Validation          [10-14h]             │
└─────────────────────────────────────────────────────────────┘
Total: 20-28 hours (3-4 days)

Priority 3 (Enhanced Features):
┌─────────────────────────────────────────────────────────────┐
│ Phase 7: Workflow Background Execution [8-10h]              │
│ Phase 8: UI Integration                [4-6h]               │
│ Phase 10: Monitoring & Documentation   [4-6h]               │
└─────────────────────────────────────────────────────────────┘
Total: 16-22 hours (2-3 days)

TOTAL: 66-92 hours (2-3 weeks)
```

## Component Dependency Graph

```
┌──────────────────────┐
│ Server Tool Registry │ ◄── Start here (Phase 1)
└───────┬──────────────┘
        │
        ├─── Required by: Tool Executor
        └─── Required by: Workflow Executor
        
┌──────────────────────┐
│ Database Schema      │ ◄── Phase 2
└───────┬──────────────┘
        │
        ├─── Required by: Tool Executor
        ├─── Required by: Stream Handler
        └─── Required by: UI Integration
        
┌──────────────────────┐
│ Tool Executor        │ ◄── Phase 3
└───────┬──────────────┘     (Depends on: Registry + Schema)
        │
        └─── Required by: Stream Handler
        
┌──────────────────────┐
│ Stream Handler       │ ◄── Phase 4
└───────┬──────────────┘     (Depends on: Executor + Schema)
        │
        ├─── Enables: Background tool execution
        └─── Required by: Workflow Executor
        
┌──────────────────────┐
│ Memory Leak Fixes    │ ◄── Phase 5 (Can run parallel)
│ Performance Opts     │ ◄── Phase 6 (Can run parallel)
└──────────────────────┘
        
┌──────────────────────┐
│ Workflow Executor    │ ◄── Phase 7
└──────────────────────┘     (Depends on: Stream Handler)
        
┌──────────────────────┐
│ UI Integration       │ ◄── Phase 8
│ Testing              │ ◄── Phase 9
│ Documentation        │ ◄── Phase 10
└──────────────────────┘
```

## Testing Strategy Pyramid

```
                    ┌──────────────┐
                    │  E2E Tests   │ ◄── User workflows
                    │   (10-14h)   │     (navigate away, return)
                    └──────┬───────┘
                           │
                    ┌──────┴────────┐
                    │ Integration   │ ◄── Full flow tests
                    │  Tests (6h)   │     (client → server → DB)
                    └───────┬───────┘
                            │
                    ┌───────┴────────┐
                    │  Unit Tests    │ ◄── Component tests
                    │   (10-14h)     │     (registry, executor, etc.)
                    └────────────────┘

Total Testing: 26-34 hours (included in Phase 9)
```

## Risk Heatmap

```
High Impact, High Probability:
┌────────────────────────────────────────────────────┐
│ • Tool execution loop complexity                   │ ◄── Mitigation: Extensive tests
│ • Memory leaks persist despite fixes               │ ◄── Mitigation: Heap snapshots
└────────────────────────────────────────────────────┘

High Impact, Low Probability:
┌────────────────────────────────────────────────────┐
│ • Convex write contention at scale                 │ ◄── Mitigation: Batching + monitoring
│ • Tool timeouts block all jobs                     │ ◄── Mitigation: Per-tool AbortSignal
└────────────────────────────────────────────────────┘

Low Impact, High Probability:
┌────────────────────────────────────────────────────┐
│ • UI integration requires iteration                │ ◄── Mitigation: Feature flag
│ • Documentation needs updates                      │ ◄── Mitigation: Write as you code
└────────────────────────────────────────────────────┘
```

## Success Metrics Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│                  Feature Success Metrics                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Functionality:                                              │
│  [✅] Tools execute in background       (Pass/Fail)          │
│  [✅] Multi-turn conversations work     (Pass/Fail)          │
│  [✅] Workflows execute with tools      (Pass/Fail)          │
│                                                              │
│  Performance:                                                │
│  [📊] SSE events per response           Target: <20          │
│  [📊] SSE latency (p95)                 Target: <100ms       │
│  [📊] Job completion time               Target: <5min        │
│                                                              │
│  Reliability:                                                │
│  [📊] Memory usage after 100 jobs       Target: Stable       │
│  [📊] Tool execution error rate         Target: <1%          │
│  [📊] Background job success rate       Target: >95%         │
│                                                              │
│  Quality:                                                    │
│  [📊] Unit test coverage                Target: >80%         │
│  [📊] Integration test coverage         Target: >70%         │
│  [✅] All E2E tests pass                (Pass/Fail)          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Quick Decision Tree

```
Should I implement this feature?

┌─────────────────────────────────────────┐
│ Is background streaming already working? │
└─────────────┬───────────────────────────┘
              │
              ▼
           Yes/No
              │
              ├─ No → Fix background streaming first (see planning/background-streaming/)
              │
              └─ Yes → Continue
                       │
                       ▼
┌──────────────────────────────────────────┐
│ Do tools work in foreground (client)?    │
└─────────────┬────────────────────────────┘
              │
              ▼
           Yes/No
              │
              ├─ No → Implement client tool registry first
              │
              └─ Yes → ✅ Ready to implement background tool execution!
                       │
                       └─ Start with QUICKSTART.md
```

---

**Legend:**
- ✅ = Must implement
- ⚠️ = Needs attention
- ❌ = Currently broken
- 📊 = Measurable metric
- ◄── = Depends on / Requires
