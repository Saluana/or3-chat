/**
 * Auto-importable re-exports of the error utility API (Task 1.3)
 * Plugin authors can now use: err, reportError, asAppError, isAppError, simpleRetry, useErrorToasts
 */
export {
    err,
    reportError,
    asAppError,
    isAppError,
    simpleRetry,
    useErrorToasts,
} from '../utils/errors';
