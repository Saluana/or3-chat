/**
 * @module app/utils/files-constants
 *
 * Purpose:
 * Re-exports file-related constants so UI and DB stay in sync.
 */

// Source of truth defined in app/db/files-util.ts
export { MAX_FILES_PER_MESSAGE } from '../db/files-util';
